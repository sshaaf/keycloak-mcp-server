# Test configuration for Keycloak
quarkus.keycloak.admin-client.server-url=${keycloak.url:http://localhost:8081}
quarkus.keycloak.devservices.enabled=false

# Logging configuration for tests
quarkus.log.category."org.testcontainers".level=INFO
quarkus.log.category."dev.shaaf.keycloak.mcp.server".level=DEBUG