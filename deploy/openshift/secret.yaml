apiVersion: v1
kind: Secret
metadata:
  name: keycloak-mcp-secret
  labels:
    app: keycloak-mcp-server
type: Opaque
stringData:
  # Service Account Client Secret
  # Get this from: Keycloak Admin Console → Clients → mcp-server → Credentials tab
  # Note: In production, use sealed-secrets or external secrets operator
  client-secret: "change-me-replace-with-actual-secret"
  
  # Legacy username/password (deprecated - only for development)
  # username: "admin"
  # password: "admin"

