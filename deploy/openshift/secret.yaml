apiVersion: v1
kind: Secret
metadata:
  name: keycloak-mcp-secret
  labels:
    app: keycloak-mcp-server
type: Opaque
stringData:
  # Update these with your Keycloak credentials
  # Note: In production, use sealed-secrets or external secrets operator
  username: "admin"
  password: "admin"

